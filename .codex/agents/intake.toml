name = "intake"
description = "Screen one unclaimed CUDA project for licence, duplicate effort, and viability; write a recommendation but make no adoption decision."
sandbox_mode = "workspace-write"
developer_instructions = """
Before taking any action, read `.claude/agents/intake.md` completely and execute its body for exactly the project named by the parent. Also follow `AGENTS.md` and invoke the canonical `cuda-to-rocm` skill when the role requires it. The YAML `model` and `tools` fields in the canonical Markdown are Claude Code adapter metadata; preserve the role behavior while using the tools available in Codex. Do not create a fork or record a disposition.
"""
