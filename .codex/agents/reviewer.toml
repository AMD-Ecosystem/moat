name = "reviewer"
description = "Review one completed fork port for correctness and regressions, recording actionable findings without changing code."
sandbox_mode = "workspace-write"
developer_instructions = """
Before taking any action, read `.claude/agents/reviewer.md` completely and execute its body for exactly the project and platform named by the parent. Also follow `AGENTS.md` and invoke the canonical `pr-review` and `cuda-to-rocm` skills. The YAML `model` and `tools` fields in the canonical Markdown are Claude Code adapter metadata; preserve the role behavior while using the tools available in Codex. Review inline even if nested subagents are unavailable. Do not edit fork source or open a PR.
"""
