{
  "schema_version": 1,
  "items": [
    {
      "id": "hiprt-jit-and-cache-fixes",
      "kind": "rocm-bug-report",
      "component": "hiprt",
      "summary": "Four HIPRT SDK fixes this port needs are absent from HIPRT HEAD (checked 2026-08-13 against GPUOpen-LibrariesAndSDKs/HIPRT, last push 2026-04-15) and are carried as local patches: (1) Compiler::getCacheFilename builds the JIT cache path from the raw device name, so any device whose name contains a slash -- e.g. 'AMD Instinct MI250X / MI250' -- writes into a non-existent directory and throws; sanitize '/' and '\\\\' to '_'. (2) Compiler::addCommonOpts passes no --offload-arch, so hiprtc targets a default arch set that excludes RDNA4 and BVH kernel loading fails with hipErrorInvalidImage (200) on gfx1201. (3) Compiler::buildProgram passes the full absolute path as the hiprtcCreateProgram source name; on Windows comgr fails silently (ret=6, empty log) when it starts with a drive letter, so pass the basename. (4) hiprt.cpp / hiprt_libpath.h / Orochi hipew.cpp use plain LoadLibraryA, which follows the legacy search order and picks the System32 display-driver amdhip64/hiprtc over the ROCm SDK copy; build full paths from ROCM_PATH/HIP_PATH. All four are genuine HIPRT bugs affecting any HIPRT user, not port-specific workarounds. Contributing them to GPUOpen is the durable fix and removes the 'apply this patch first' step from the port's ROCm build instructions.",
      "refs": [
        "projects/diff-surfel-tracing/plan.md",
        "projects/EnvGS/hiprt-feasibility.md"
      ],
      "status": "open",
      "upstream_issue": null,
      "created_at": "2026-08-13T04:04:03Z",
      "decided": null
    },
    {
      "id": "hiprt-collapse-reference-count-hang",
      "kind": "rocm-bug-report",
      "component": "hiprt",
      "summary": "HIP RT's BVH Collapse kernel (hiprt/impl/BvhBuilderKernels.h) hangs the GPU on some geometry at tag 3.1.0.cb09c56. It is a persistent task-queue kernel, and a lane that never receives a task can only leave through 'atomicAdd(&header->m_referenceCount, 0) == referenceCount'. The counter is monotonic, so the exact comparison is missed permanently once the emitted count passes referenceCount -- and that is what happens: relaxing it to >= makes every hanging scene build and trace correctly, which proves the collapse emits more references than the primitive count it was given. referenceIndices is allocated with exactly primitives.getCount() entries, so the same overshoot also writes past the end of that temporary buffer, and >= alone would convert a hang into silent corruption rather than fix it. Reproduces from about 96 triangles (42 surfel disks) upward, is data dependent rather than size dependent (44 disks builds, 42 and 46 do not), and affects the LBVH, LBVH-without-triangle-pairing and PLOC builders alike because all three call the same Collapse kernel. It affects any HIP RT user whose geometry trips the miscount. Needs the miscount root-caused, then reported to GPUOpen.",
      "refs": [
        "projects/diff-surfel-tracing/notes.md"
      ],
      "status": "open",
      "upstream_issue": null,
      "created_at": "2026-08-13T06:41:03Z",
      "decided": {
        "choice": "now",
        "by": "jeffdaily",
        "at": "2026-08-13T18:42:16Z"
      }
    }
  ]
}
